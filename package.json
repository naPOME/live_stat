{
  "name": "live-stat-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.6.0",
  "scripts": {
    "dev": "turbo dev",
    "dev:local": "pnpm --filter local dev",
    "dev:cloud": "pnpm --filter cloud dev",
    "build": "turbo build",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fortawesome/react-fontawesome": "^3.2.0"
  }
}
